import {createMuiTheme} from '@material-ui/core/styles';

const theme = createMuiTheme({
  bgColor: {
    red: "#ab000d",
    green: "#087f23",
    purple: "#4527a0"
  },
  color: {
    white: "#fff",
    black: "#000"
  }
});

export default theme;